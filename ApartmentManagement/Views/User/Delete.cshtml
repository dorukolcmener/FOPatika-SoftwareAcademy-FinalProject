@model UserViewModel

@{
    ViewData["Title"] = "Users Page";
}

<div class="text-center d-flex justify-content-center">
    <div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">User @Model.Id</h5>
        <p class="card-text">Performing this action will also delete all the apartments and bills that user is associated with!</p>
    </div>
    <ul class="list-group list-group-flush">
        <li class="list-group-item">Are you sure that you want to delete the user?</li>
        <li class="list-group-item">@Model.Name @Model.Surname</li>
    </ul>
    <div class="card-body d-flex flex-row justify-content-around">
        <form method="post">
        <input type="hidden" name="id" value=@Model.Id />
        <button class="btn btn-danger">Delete</button>
        </form>
        <a href="#" class="btn btn-secondary" onclick="history.go(-1);">Cancel</a>
    </div>
    </div>
</div>